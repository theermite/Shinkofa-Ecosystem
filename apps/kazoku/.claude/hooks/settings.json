{
  "$schema": "https://claude.ai/schemas/hooks-settings.json",
  "description": "Configuration Hooks Claude Code - Jay The Ermite",
  "version": "1.0.0",

  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write",
        "description": "Vérifier que le fichier n'est pas sensible avant écriture",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Writing to file...'",
            "comment": "Placeholder - peut être remplacé par un script de validation"
          }
        ]
      }
    ],

    "PostToolUse": [
      {
        "matcher": "Write",
        "description": "Auto-lint après écriture de fichiers",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'File written, consider running linter'",
            "comment": "Remplacer par: npx eslint --fix ${file} ou ruff check --fix ${file}"
          }
        ]
      },
      {
        "matcher": "Edit",
        "description": "Rappel de tester après modification",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'File edited - remember to run tests'",
            "comment": "Reminder automatique"
          }
        ]
      }
    ],

    "Stop": [
      {
        "description": "Log fin de session",
        "hooks": [
          {
            "type": "command",
            "command": "echo '[SESSION END] %date% %time%'",
            "comment": "Peut être étendu pour logger dans un fichier"
          }
        ]
      }
    ]
  },

  "notes": {
    "activation": "Copier ce fichier dans .claude/settings.json du projet",
    "customization": "Adapter les commandes selon le projet (eslint, ruff, pytest, etc.)",
    "windows": "Utiliser syntaxe PowerShell si nécessaire",
    "linux": "Les commandes bash fonctionnent directement"
  }
}
